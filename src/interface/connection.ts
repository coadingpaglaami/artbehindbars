export interface Connection {
  id: string; // Unique identifier for the connection
  name: string; // Name of the connection
  totalConnections: number; // Total number of connections the user has
  isBlocked: boolean; //
}
